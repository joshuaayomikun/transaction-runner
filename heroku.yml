build: 
  docker:
    worker: Dockerfile.heroku
run:
  web: bundle exec puma -C config/puma.rb
  worker: npm start ./build/bin/www.js
  asset-syncer:
    command:
      - npm start ./build/bin/www.js
    image: worker
